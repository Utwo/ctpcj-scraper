{
  "name": "scraper",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "generate": "rm -rf static && mkdir static && node scraper.js",
    "deploy": "now --target=production --token=${NOW_TOKEN}"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "async-request": "^1.2.0",
    "cheerio": "^1.0.0-rc.2",
    "csvtojson": "^1.1.9",
    "jsonfile": "^4.0.0"
  },
  "devDependencies": {
    "now": "^15.0.7"
  }
}
